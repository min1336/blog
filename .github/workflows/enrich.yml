name: Issue Enrich

on:
  workflow_call:
    secrets:
      CLAUDE_OAUTH_TOKEN:
        required: true

jobs:
  enrich:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - run: echo "Enrich triggered for issue event"
